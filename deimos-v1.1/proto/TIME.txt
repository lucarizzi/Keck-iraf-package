include	<time.h>

char	tm_stamp[SZ_TIME]			# time-stamp string
long	clktime()

	call cnvtime (clktime (0), tm_stamp, SZ_TIME)

include	<time.h>
procedure tm_stamp (tm_str, n)

char	tm_str[n]
int	n

long	clktime()
begin
	call cnvtime (clktime (0), tm_str, n)
end
